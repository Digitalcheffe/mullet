// Admin shell. Pages (setup wizard, dashboard, plugin management, display
// designer, etc.) land here as later issues build them out.
export default function AdminApp() {
  return (
    <div className="admin-app">
      <h1>Mullet Admin</h1>
      <p>Admin UI scaffold — pages are added in later issues.</p>
    </div>
  );
}
